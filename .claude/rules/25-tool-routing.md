---
paths:
  - "**/*"
---

Use gitnexus as the default path to discover code: graph-aware orientation
(`query`), symbol relationships (`context`, `trace`), and blast-radius
analysis (`impact`). Fall back to `rg` for exact text or path searches, and
repomix only for bulk snapshots. Use `gh` for GitHub
operations, context7 for current library docs, and mcp-package-version before
adding dependencies.
