# Replay-safe contract allowlist (spec 021, T037 / A-021-4).
#
# Contracts listed here may set replaySafe=true in their schema.
# All other contracts default to replaySafe=false.
#
# Rules (CI lint enforced):
# - EVERY contract schema must declare replaySafe explicitly (fail if missing).
# - Write contracts (ui-to-core, state-mutating) MUST NOT set replaySafe=true
#   unless the contract name appears in this file.
# - Read-only contracts may set replaySafe=true without appearing here.
#
# To add a write contract to this allowlist, document the reason below and
# get review — treat this file as a security-relevant configuration.
#
# Format: one operation name per line. Lines starting with # are comments.
#
# (no write contracts currently allowed replay; extend as needed)
