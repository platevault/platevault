{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://astro-plan.local/contracts/lifecycle.transition.json",
  "title": "lifecycle.transition",
  "description": "Operation contract for proposing and applying a lifecycle state transition on a Data Asset. Source-of-truth schema for the Rust contract DTOs in crates/contracts/core/ and the generated TypeScript surface in packages/contracts/generated/.",
  "type": "object",
  "oneOf": [
    { "$ref": "#/$defs/Request" },
    { "$ref": "#/$defs/Response" }
  ],
  "$defs": {
    "ContractVersion": {
      "type": "string",
      "const": "2.0.0"
    },
    "Uuid": {
      "type": "string",
      "format": "uuid"
    },
    "Timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "Actor": {
      "type": "string",
      "description": "Initiator of the transition. `actor=system` is only permitted on edges entering or leaving `blocked` (per spec 009 ratification). Use-case layer enforces the rule and rejects violations with `transition.refused`.",
      "enum": ["user", "system"]
    },
    "EntityType": {
      "type": "string",
      "enum": [
        "library_root",
        "file_record",
        "acquisition_session",
        "calibration_session",
        "data_source",
        "project",
        "prepared_source",
        "processing_artifact",
        "projection",
        "plan",
        "inventory_session",
        "filesystem_plan"
      ]
    },
    "ProjectState": {
      "type": "string",
      "enum": [
        "setup_incomplete",
        "ready",
        "prepared",
        "processing",
        "completed",
        "archived",
        "blocked"
      ]
    },
    "PlanState": {
      "type": "string",
      "enum": [
        "draft",
        "ready_for_review",
        "approved",
        "applying",
        "applied",
        "partially_applied",
        "failed",
        "cancelled",
        "discarded"
      ]
    },
    "SessionState": {
      "type": "string",
      "enum": [
        "discovered",
        "candidate",
        "needs_review",
        "confirmed",
        "rejected",
        "ignored"
      ]
    },
    "DataSourceState": {
      "type": "string",
      "enum": ["active", "missing", "disabled", "reconnect_required"]
    },
    "FileRecordState": {
      "type": "string",
      "enum": [
        "observed",
        "missing",
        "changed",
        "classified",
        "rejected",
        "protected"
      ]
    },
    "PreparedSourceState": {
      "type": "string",
      "enum": ["not_created", "planned", "ready", "stale", "retired"]
    },
    "ProjectionState": {
      "type": "string",
      "enum": ["current", "stale", "regenerating"]
    },
    "ProjectRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "project" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/ProjectState" },
        "nextState": { "$ref": "#/$defs/ProjectState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "PlanRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "plan" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/PlanState" },
        "nextState": { "$ref": "#/$defs/PlanState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "InventorySessionRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "inventory_session" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/SessionState" },
        "nextState": { "$ref": "#/$defs/SessionState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "CalibrationSessionRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "calibration_session" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/SessionState" },
        "nextState": { "$ref": "#/$defs/SessionState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "DataSourceRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "data_source" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/DataSourceState" },
        "nextState": { "$ref": "#/$defs/DataSourceState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "PreparedSourceRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "prepared_source" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/PreparedSourceState" },
        "nextState": { "$ref": "#/$defs/PreparedSourceState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "ProjectionRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "projection" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/ProjectionState" },
        "nextState": { "$ref": "#/$defs/ProjectionState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "FileRecordRequest": {
      "type": "object",
      "required": [
        "contractVersion",
        "requestId",
        "entityType",
        "entityId",
        "currentState",
        "nextState",
        "actor"
      ],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "entityType": { "type": "string", "const": "file_record" },
        "entityId": { "$ref": "#/$defs/Uuid" },
        "currentState": { "$ref": "#/$defs/FileRecordState" },
        "nextState": { "$ref": "#/$defs/FileRecordState" },
        "actionLabel": { "$ref": "#/$defs/ActionLabel" },
        "actor": { "$ref": "#/$defs/Actor" }
      }
    },
    "ActionLabel": {
      "type": "string",
      "description": "Human-readable label recorded on the audit entry (e.g. 'Unarchived').",
      "minLength": 1,
      "maxLength": 120
    },
    "LifecycleState": {
      "description": "Any per-family lifecycle state value. Use the family-specific enums in the discriminated Request sub-schemas; this alias exists only for the Response shape where the family is fixed by the corresponding request.",
      "oneOf": [
        { "$ref": "#/$defs/ProjectState" },
        { "$ref": "#/$defs/PlanState" },
        { "$ref": "#/$defs/SessionState" },
        { "$ref": "#/$defs/DataSourceState" },
        { "$ref": "#/$defs/FileRecordState" },
        { "$ref": "#/$defs/PreparedSourceState" },
        { "$ref": "#/$defs/ProjectionState" }
      ]
    },
    "ErrorCode": {
      "type": "string",
      "enum": [
        "transition.refused",
        "entity.not_found",
        "actor.not_authorised",
        "plan.required",
        "plan.not_approved",
        "provenance.unreviewed"
      ]
    },
    "ErrorEnvelope": {
      "type": "object",
      "required": ["code", "message"],
      "additionalProperties": false,
      "properties": {
        "code": { "$ref": "#/$defs/ErrorCode" },
        "message": {
          "type": "string",
          "description": "Human-readable explanation. MUST NOT include secrets or absolute filesystem paths."
        },
        "details": {
          "type": "object",
          "description": "Optional structured detail. Examples: { allowedNextStates: [...] } for transition.refused; { planId: uuid } for plan.required; { blockingFields: [{ fieldPath, requiredOrigin: 'reviewed' }] } for provenance.unreviewed.",
          "additionalProperties": true,
          "properties": {
            "allowedNextStates": {
              "type": "array",
              "items": { "type": "string" }
            },
            "planId": { "$ref": "#/$defs/Uuid" },
            "blockingFields": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["fieldPath", "requiredOrigin"],
                "additionalProperties": false,
                "properties": {
                  "fieldPath": { "type": "string" },
                  "requiredOrigin": {
                    "type": "string",
                    "enum": ["reviewed"]
                  }
                }
              }
            }
          }
        }
      }
    },
    "Request": {
      "description": "Discriminated request envelope. Each branch pins `entityType` to a single family and constrains `currentState`/`nextState` to that family's enum. The server derives `requiresPlan` from a canonical (entityType, from, to) edge table; callers MUST NOT assert it.",
      "oneOf": [
        { "$ref": "#/$defs/ProjectRequest" },
        { "$ref": "#/$defs/PlanRequest" },
        { "$ref": "#/$defs/InventorySessionRequest" },
        { "$ref": "#/$defs/CalibrationSessionRequest" },
        { "$ref": "#/$defs/DataSourceRequest" },
        { "$ref": "#/$defs/PreparedSourceRequest" },
        { "$ref": "#/$defs/ProjectionRequest" },
        { "$ref": "#/$defs/FileRecordRequest" }
      ]
    },
    "Response": {
      "type": "object",
      "description": "Response envelope for lifecycle.transition.",
      "required": ["status", "contractVersion", "requestId"],
      "additionalProperties": false,
      "properties": {
        "status": {
          "type": "string",
          "enum": ["success", "noop", "error"],
          "description": "`success` = state mutated and audit row written. `noop` = caller-requested nextState equals currentState; no mutation, no audit row, no error. `error` = transition refused or otherwise rejected; structured `error` field populated."
        },
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "appliedAt": { "$ref": "#/$defs/Timestamp" },
        "priorState": { "$ref": "#/$defs/LifecycleState" },
        "newState": { "$ref": "#/$defs/LifecycleState" },
        "auditId": { "$ref": "#/$defs/Uuid" },
        "planId": {
          "$ref": "#/$defs/Uuid",
          "description": "Set when the server created or referenced a FilesystemPlan whose approval gates the transition."
        },
        "error": { "$ref": "#/$defs/ErrorEnvelope" }
      },
      "allOf": [
        {
          "if": { "properties": { "status": { "const": "success" } } },
          "then": {
            "required": ["appliedAt", "newState", "auditId"],
            "not": { "required": ["error"] }
          }
        },
        {
          "if": { "properties": { "status": { "const": "noop" } } },
          "then": {
            "not": {
              "anyOf": [
                { "required": ["auditId"] },
                { "required": ["error"] }
              ]
            }
          }
        },
        {
          "if": { "properties": { "status": { "const": "error" } } },
          "then": { "required": ["error"] }
        }
      ]
    }
  }
}
