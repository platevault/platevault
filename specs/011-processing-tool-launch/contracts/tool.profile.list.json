{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://astro-plan.local/contracts/tool.profile.list.json",
  "title": "tool.profile.list",
  "description": "List the supported processing-tool profiles, joined with Settings-owned executable paths and enabled flags. Used by the Settings → Tool Workflows section and by the project CTA to decide whether 'Open in {tool}' is enabled.",
  "type": "object",
  "oneOf": [
    { "$ref": "#/$defs/Request" },
    { "$ref": "#/$defs/Response" }
  ],
  "$defs": {
    "ContractVersion": { "type": "string", "const": "2.0.0" },
    "Uuid": { "type": "string", "format": "uuid" },
    "ToolId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "ToolProfile": {
      "type": "object",
      "description": "Settings-joined view of a tool profile. Static fields come from crates/workflow/profiles/; mutable fields come from Settings.",
      "required": [
        "id",
        "name",
        "configured",
        "available",
        "supportsOpenFolder",
        "enabled",
        "autoDetected"
      ],
      "additionalProperties": false,
      "properties": {
        "id": { "$ref": "#/$defs/ToolId" },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "description": "Display name shown in Settings and in the CTA label."
        },
        "configured": {
          "type": "boolean",
          "description": "True when executablePath is set and non-blank in Settings."
        },
        "available": {
          "type": "boolean",
          "description": "True when configured AND the executable currently exists with execute permission. Filesystem read; debounce at the UI layer."
        },
        "supportsOpenFolder": {
          "type": "boolean",
          "description": "Whether the tool's argsTemplate renders {folder}. The CTA still passes cwd when this is false."
        },
        "enabled": {
          "type": "boolean",
          "description": "User-controlled switch for hiding the tool from project CTAs without deleting the path."
        },
        "autoDetected": {
          "type": "boolean",
          "description": "True when the current executablePath came from R2 discovery and has not been explicitly saved by the user yet."
        }
      }
    },
    "Request": {
      "type": "object",
      "description": "List all known tool profiles. No filters in v1.",
      "required": ["contractVersion", "requestId"],
      "additionalProperties": false,
      "properties": {
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" }
      }
    },
    "Response": {
      "type": "object",
      "required": ["status", "contractVersion", "requestId"],
      "additionalProperties": false,
      "properties": {
        "status": { "type": "string", "enum": ["success", "error"] },
        "contractVersion": { "$ref": "#/$defs/ContractVersion" },
        "requestId": { "$ref": "#/$defs/Uuid" },
        "tools": {
          "type": "array",
          "items": { "$ref": "#/$defs/ToolProfile" }
        },
        "error": {
          "type": "object",
          "required": ["code", "message"],
          "additionalProperties": false,
          "properties": {
            "code": { "type": "string" },
            "message": { "type": "string" }
          }
        }
      },
      "allOf": [
        {
          "if": { "properties": { "status": { "const": "success" } } },
          "then": {
            "required": ["tools"],
            "not": { "required": ["error"] }
          }
        },
        {
          "if": { "properties": { "status": { "const": "error" } } },
          "then": { "required": ["error"] }
        }
      ]
    }
  }
}
