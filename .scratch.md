# P1 (052-p1-facade-cache) — working notes

## Setup
- Branched `052-p1-facade-cache` off `origin/main` (2622b481).
- Read spec.md/plan.md/tasks.md/data-model.md/research.md at
  specs/052-simbad-caching-dual-lookup-cone-search/, DECISIONS.md.
- Read simbad-resolver 0.2.0 source at /home/sjors/dev/simbad-resolver (local
  checkout, not yet published state matches Cargo.toml version 0.2.0):
  - `SimbadResolver<R: Resolver>` facade (src/orchestrate.rs): `new(resolver,
    CacheBackend, ResolverConfig)`, `.resolve()` (cache-first, Caldwell-aware,
    persists to its own redb Cache on a resolver hit — never fabricates),
    `.search()` (exact/prefix/substring + optional fuzzy), `.apply_override()`.
  - `CacheBackend::File(FileCache::new(path))` / `::file(path)` — one redb
    Store, no TTL knob at all (matches "no TTL" decision — nothing to disable).
  - `ResolvedIdentity` gained `otype_raw: String` + `v_mag: Option<f64>`.
  - `wire::parse_basic_row` is now a 6-tuple `(oid, main_id, ra, dec, otype,
    v_mag: Option<f64>)` — seed-builder fix target.
  - Crate's own `Resolver`/`FakeResolver`/`OfflineResolver`/`PositionResolver`
    traits are SEPARATE from astro-plan's local copies in
    targeting_resolver::{Resolver, FakeResolver, ...} (lib.rs) — the facade is
    generic over the CRATE's `Resolver` trait, not ours.

## Scope-vs-spec-intent conflict found (blocking)

Traced every "in use" persistence-gate site required by FR-004/data-model.md
INV-1 and SC-002 ("browse without adopting → zero canonical_target rows"):

1. **Favourite** (`crates/app/targets/src/target_favourites.rs:59` `add()`) —
   in scope. Already requires target pre-exist in canonical_target
   (`target_exists` check) — currently satisfied because `target.resolve`
   writes eagerly today.
2. **Linked to acquisition session** — actually
   `crates/app/targets/src/ingest_resolution.rs` `associate_or_enqueue` /
   `resolve_pending`, called from `ingest_sessions.rs` — in scope, and this
   IS already gated correctly: it only runs when a light frame is actually
   ingested (i.e., an applied/confirmed Inbox plan), so it's a legitimate
   in-use write point already.
3. **Added to a project** — `crates/app/projects/src/project_setup.rs:448`
   `canonical_target_exists()` gates `project.create`; that crate is OUT of
   my P1 scope AND not covered by any node in run-052's DAG (P1: resolver +
   app/targets + seed-builder + persistence/db; P2: app/targets; P3:
   packages/contracts). Nothing in this run touches `crates/app/projects`.
4. **The actual write that satisfies (3) today** happens via
   `apps/desktop/src/components/TargetSearch/TargetSearch.tsx:262` —
   `commands.targetResolve` fires on every **debounced typeahead keystroke**
   (`MIN_RESOLVE_LEN=3`, `DEBOUNCE_MS=300`, TargetSearch.tsx:69/72), NOT only
   on selection/confirm. `CreateProjectDialog`'s embedded `TargetSearch` gets
   its `target_id` from this already-resolved suggestion and later submits
   `project.create` with `canonicalTargetId` — relying on the eager write.
   `AddTargetDialog.tsx` similarly calls `target.resolve` only on explicit
   "Add Target" confirm (that one IS a legitimate in-use moment) but shares
   the same `target.resolve` backend command as the typeahead path.

Consequence: `target.resolve`'s SQLite write cannot be moved to a true
in-use gate without also touching `crates/app/projects` (FK precondition)
and/or `apps/desktop/src/features/projects/**` /
`apps/desktop/src/components/TargetSearch/**` (to stop resolving-and-adopting
on every keystroke, or to add an explicit adopt call at project-create time)
— none of which are in P1 scope or anywhere in run-052's DAG. Sent
`BLOCKED P1 kind:design` to main with this evidence instead of guessing.
