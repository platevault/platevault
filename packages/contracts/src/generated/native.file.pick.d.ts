// Copyright (C) 2024-2026 Sjors Robroek
// SPDX-License-Identifier: AGPL-3.0-only

/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Open the OS-native file picker with type filters and return the user-selected absolute path. Cancellation is a non-error null response. Implemented over @tauri-apps/plugin-dialog with directory=false, multiple=false. Used by master-calibration selection for FITS, XISF, and TIFF files.
 */
export interface NativeFilePick {
  request: {
    contractVersion: string;
    requestId: string;
    /**
     * Ordered list of file-type filters. The first filter is the default. Extensions MUST NOT include the leading dot.
     *
     * @minItems 1
     */
    filters: [
      {
        name: string;
        /**
         * Extensions without leading dot. The wildcard '*' is ONLY valid in a filter named exactly 'All files'; the server returns filters.invalid if '*' appears in any other filter row (D-004-1).
         *
         * @minItems 1
         */
        extensions: [string, ...string[]];
      },
      ...{
        name: string;
        /**
         * Extensions without leading dot. The wildcard '*' is ONLY valid in a filter named exactly 'All files'; the server returns filters.invalid if '*' appears in any other filter row (D-004-1).
         *
         * @minItems 1
         */
        extensions: [string, ...string[]];
      }[]
    ];
    /**
     * Optional absolute path to anchor the dialog (typically the calibration source root). Honored where supported.
     */
    defaultPath?: string | null;
  };
  response:
    | {
        status: "success";
        contractVersion: string;
        requestId: string;
        /**
         * Absolute OS-canonical path the user selected, or null when the user cancelled.
         */
        path: string | null;
        /**
         * The name of the filter active when the user clicked Open. Useful for recording the user's declared frame type.
         */
        selectedFilter?: string | null;
        cancelled: boolean;
      }
    | {
        status: "failure";
        contractVersion: string;
        requestId: string;
        errors: Error[];
      };
  [k: string]: unknown;
}
/**
 * This interface was referenced by `NativeFilePick`'s JSON-Schema
 * via the `definition` "Error".
 */
export interface Error {
  code: "picker.unavailable" | "filters.invalid" | "os.command_failed";
  message: string;
  details?: {
    [k: string]: unknown;
  };
}
