# Contracts

Language-neutral contract source files and generated bindings will live here.

The SpecKit plan must decide the source-of-truth format before schemas are
added. Candidate formats include JSON Schema, OpenAPI, AsyncAPI-style operation
schemas, or a hybrid suited to local Tauri commands and future remote backends.
