# q16-t130 — J12-failure-refusal-handling: Unresolved chip is visually distinct from an error

Task: q16-t130 · Status: merged-spec
Wave 0 grilling: Q16 · missing-value semantics & detail-as-delta (#620/#619, FR-135–FR-140, spec §12)

## Behavior delta (this journey)

The shared `renderValue` unresolved chip is a muted, distinct component (i18n copy in the Paraglide catalog) — it must not be confusable with a failure/error indicator surfaced during refusal handling.

## Stages hit

- Stage 3 "A partial apply failure lists failed items by name with per-item reasons".

## Reviewer verification

1. Render a missing value and an error state side by side; assert they are visually and semantically distinct (unresolved chip != error badge).
2. Assert the unresolved chip copy comes from the i18n catalog (no raw key, no English-only leak).

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: gap — `journeys/failure-refusal-handling` scenario to be authored
- Manual-Windows: n/a
- Coverage-matrix: #18
