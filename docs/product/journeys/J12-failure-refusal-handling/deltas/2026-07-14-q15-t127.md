# q15-t127 — J12-failure-refusal-handling: Refused/failed mutations are durably audited

Task: q15-t127 · Status: merged-spec
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

Refused and failed mutations now produce durable `audit_log_entry` rows with outcome + reason/code — the refusal reason the UI shows is the same one stored durably.

## Stages hit

- Stage 1 "A refused lifecycle transition surfaces its refusal reason inline … the same reason the audit record stores".
- Stage 5 "Every refusal/failure is afterwards findable in the Audit Log with outcome refused/failed and the same reason on demand".

## Reviewer verification

1. Trigger a refused lifecycle transition; assert a durable row with outcome=refused and a `reason_code`; assert UI reason text == stored reason.
2. Trigger a partial apply failure; assert failed items have durable rows with reasons.
3. Negative (FR-134): reads, navigation, and UI-state changes produce ZERO audit rows.

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: gap — `journeys/failure-refusal-handling` scenario to be authored
- Manual-Windows: n/a
- Coverage-matrix: #18
