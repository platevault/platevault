# q15-t124 — J15-equipment-observing-site-setup: Equipment CRUD is audited

Task: q15-t124 · Status: merged-spec
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

Camera/telescope/optical-train/filter add/edit/remove now writes durable audit rows (`crates/app/calibration/src/equipment.rs` previously emitted no audit at all).

## Stages hit

- Stage 1 "Settings → Equipment: register cameras and telescopes … compose optical trains" — each CRUD op is now audited.
- Stage 2 "Filters: adjust the seeded list" — filter edits are audited.

## Reviewer verification

1. Add a camera; assert an `audit_log_entry` row (workflow severity, actor=user, action=create, entity=equipment).
2. Edit then remove it; assert an update row and a delete row, each with before→after where applicable.
3. Negative: opening the Equipment pane (a read) writes no audit row.

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: gap — `journeys/equipment-site-setup` scenario to be authored
- Manual-Windows: n/a
- Coverage-matrix: — (no dedicated coverage area)
