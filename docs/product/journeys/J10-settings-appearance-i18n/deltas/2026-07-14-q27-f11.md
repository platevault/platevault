# q27-f11 — J10-settings-appearance-i18n: Clustering tunables in Settings (NEW)

Task: q27-f11 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

The R11a clustering tunables (pointing fraction of FOV, rotation degrees, mosaic envelope factor, no-FOV fallback angle) are persisted in Settings with the R11a defaults and surfaced in the Settings UI.

## Stages hit

- Stage 1 "Settings groups … panes … Every pane auto-saves" — a new set of framing clustering tunables appears (likely under Ingestion/session grouping). Journey doc update needed.

## Reviewer verification

1. Assert the four tunables persist with R11a defaults (pointing 10%, rotation 3°, mosaic envelope 1.0×FOV, fallback 0.2°) and round-trip a pane switch/restart.
2. Assert changing a tunable is auto-saved (no global Save button).

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: `settings_ui_ingestion_toggle_autosaves_no_global_save_button` (settings_journeys.rs) — extend for tunables
- Manual-Windows: `journey-10-*` stage 1
- Coverage-matrix: #19
