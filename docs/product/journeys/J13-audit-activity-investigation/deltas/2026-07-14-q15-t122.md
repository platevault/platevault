# q15-t122 — J13-audit-activity-investigation: Audit Log now shows settings changes

Task: q15-t122 · Status: merged-spec
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

Settings changes become first-class, queryable audit history — previously they emitted to the bus only and never appeared in the durable Audit Log.

## Stages hit

- Stage 2 "Settings → Audit Log holds the durable record" — settings mutations now appear there.
- Stage 3 "Filtering by entity/date narrows the trail" — settings entity rows are now filterable.

## Reviewer verification

1. After a settings change, open Settings → Audit Log and find the row by entity type=settings and by date; outcome + actor + before→after visible.
2. Negative: navigating between settings panes (reads) adds no rows.

## Rerun set (minimal)

- Layer-1: `plan_review_apply_with_audit` (journeys.rs) — plan events only
- Layer-2: gap — no L2 covers non-plan audit rows (settings/protection/equipment/source)
- Manual-Windows: n/a
- Coverage-matrix: #22
