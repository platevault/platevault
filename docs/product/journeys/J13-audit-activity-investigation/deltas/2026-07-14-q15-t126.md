# q15-t126 — J13-audit-activity-investigation: Activity panel reads durable audit

Task: q15-t126 · Status: blocked-on-Q9
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

The Activity/log panel is to read user-meaningful events from the durable audit store and transient noise from the live bus — making 'activity is a view over the audit' literally true. BLOCKED on the Q9 log-panel iteration (not yet specified in spec-030).

## Stages hit

- Stage 1 "The status-bar Log toggle opens the Activity panel: live stream, severity chips, follow mode".

## Reviewer verification

1. (When unblocked) Perform a settings change; assert it appears in the Activity panel sourced from the durable audit store, filterable by workflow severity.
2. Negative: transient internal/periodic events appear only from the bus, never as durable audit rows (FR-134).

## Rerun set (minimal)

- Layer-1: `plan_review_apply_with_audit` (journeys.rs) — plan events only
- Layer-2: gap — no L2 covers non-plan audit rows (settings/protection/equipment/source)
- Manual-Windows: n/a
- Coverage-matrix: #22
