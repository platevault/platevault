# q16-t129 — J08-calibration-ingest-masters-matching: De-zero calibration fingerprints and master size

Task: q16-t129 · Status: merged-spec
Wave 0 grilling: Q16 · missing-value semantics & detail-as-delta (#620/#619, FR-135–FR-140, spec §12)

## Behavior delta (this journey)

`unwrap_or(0.0)` on exposure/gain and the hardcoded `0 AS size_bytes` view are removed (view redefined to emit NULL); fabricated `"1x1"` binning, empty-string camera, and self-referential `source_session_id` sentinels are dropped — absence flows as null end-to-end.

## Stages hit

- Stage 3 "selecting a master surfaces ranked candidate sessions … real context (target, filter, night, frame count) rather than opaque ids".

## Reviewer verification

1. Assert master size renders unresolved when unknown, never "0 KB".
2. Assert candidate context never shows fabricated "1x1" binning or an empty camera string — absent values render as unresolved.
3. Regression: calibration matching results are UNCHANGED by de-zeroing (matching runs on Option-typed `SessionInfo`/`MasterInfo`, never the contract DTO).

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: `calibration_ui_confirmed_master_shows_kind_conditional_detail` (calibration_ui_journeys.rs)
- Manual-Windows: `journey-08-*` (calibration)
- Coverage-matrix: #5
