# q15-t122 — J08-calibration-ingest-masters-matching: Offset-tolerance change is audited

Task: q15-t122 · Status: merged-spec
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

Changing the calibration offset/matching tolerance (a durable-data settings key) now emits a durable audit row with before→after.

## Stages hit

- Stage 5 "An 'Offset tolerance' setting (Settings → Calibration) controls whether sessions … can match" — the change is now audited.

## Reviewer verification

1. Change Offset tolerance in Settings → Calibration; assert one `audit_log_entry` row (workflow severity) carrying old→new value.
2. Negative: merely opening the Calibration settings pane (a read) produces no audit row.

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: `calibration_ui_confirmed_master_shows_kind_conditional_detail` (calibration_ui_journeys.rs)
- Manual-Windows: `journey-08-*` (calibration)
- Coverage-matrix: #5
