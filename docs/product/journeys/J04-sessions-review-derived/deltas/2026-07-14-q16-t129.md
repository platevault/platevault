# q16-t129 — J04-sessions-review-derived: Session detail stops synthesizing absent metadata

Task: q16-t129 · Status: merged-spec
Wave 0 grilling: Q16 · missing-value semantics & detail-as-delta (#620/#619, FR-135–FR-140, spec §12)

## Behavior delta (this journey)

Session detail fields fed by the same mapping layer (camera, binning, exposure) no longer show synthesized sentinels; missing renders as unresolved.

## Stages hit

- Stage 2 "the corresponding acquisition session(s) appear automatically, with counts matching what was actually moved/catalogued".

## Reviewer verification

1. A session with missing camera renders unresolved, not an empty-string chip.
2. Negative: a real 0-valued field renders "0" with its source pill.

## Rerun set (minimal)

- Layer-1: to be written in the owning task
- Layer-2: `sessions_ui_derived_view_invariants` (sessions_journeys.rs)
- Manual-Windows: `journey-04-*` (sessions)
- Coverage-matrix: #6
