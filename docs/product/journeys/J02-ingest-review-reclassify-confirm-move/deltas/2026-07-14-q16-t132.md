# q16-t132 — J02-ingest-review-reclassify-confirm-move: Shared renderer adopted on this surface

Task: q16-t132 · Status: merged-spec
Wave 0 grilling: Q16 · missing-value semantics & detail-as-delta (#620/#619, FR-135–FR-140, spec §12)

## Behavior delta (this journey)

The shared renderer is adopted across list cells, meta lines, and composed identifying strings on this surface; missing tokens are omitted entirely (no sentinel), real/unresolved/not-applicable are distinguishable everywhere.

## Stages hit

- Stage 1 "a status-bar breakdown always matches the queue's real contents" — list cells/meta lines route through the renderer

## Reviewer verification

1. Every field-value rendering (table cell, meta line) on the surface is distinguishable as real / unresolved / not-applicable (SC-010).
2. Composed identifying strings omit absent tokens — no "—" placeholder inside a joined meta line.
3. Negative: no missing numeric ever renders as 0.

## Rerun set (minimal)

- Layer-1: `plan_review_apply_with_audit` (journeys.rs)
- Layer-2: `inbox_ui_missing_path_attribute_banner_blocks_confirm`, `inbox_ui_confirm_does_not_move_then_apply_moves_to_shown_destination` (inbox_ui_journeys.rs)
- Manual-Windows: `journey-02-*` (inbox move)
- Coverage-matrix: #3, #18
