# q27-f10 — J02-ingest-review-reclassify-confirm-move: Confirm persists the chosen attribution

Task: q27-f10 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

The confirm request gains a per-item `chosenAttribution` field; the picked framing/project membership is persisted at confirm time (creating the framing/project when the kind requires it). Membership is DB metadata — same write on move and catalogue confirms.

## Stages hit

- NEW stage inside Stage 4 "Confirm" — the user's attribution pick is written as part of the confirm request. Journey doc update needed.

## Reviewer verification

1. Confirm with chosenAttribution=add_to_framing(framingId); assert the session becomes that framing's member (SC-008 end-to-end).
2. Omit chosenAttribution; assert the session is left unassigned (attributable later via reassign).
3. Assert NO §II filesystem plan is generated for the membership write.

## Rerun set (minimal)

- Layer-1: F-Framing-10 Layer-1 (SC-008 end-to-end)
- Layer-2: gap — authored in F-Framing-9
- Manual-Windows: F-Framing-9 (to be authored) + `journey-02-*`
- Coverage-matrix: #3 (extend)
