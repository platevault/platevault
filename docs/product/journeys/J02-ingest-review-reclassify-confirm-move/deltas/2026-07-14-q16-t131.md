# q16-t131 — J02-ingest-review-reclassify-confirm-move: PropertyTable couples source badge to value presence

Task: q16-t131 · Status: merged-spec
Wave 0 grilling: Q16 · missing-value semantics & detail-as-delta (#620/#619, FR-135–FR-140, spec §12)

## Behavior delta (this journey)

`PropertyTable` adopts the shared renderer: no source badge on a missing value; not-applicable is an explicit `PropertyDef` applicability marker (from the data-model.md matrix), not null-overload.

## Stages hit

- Stage 2 "the item surfaces a needs-review state … affected rows get \"needs <attribute>\" badges" — the property table now sources presence from the renderer

## Reviewer verification

1. A missing property renders an unresolved chip with NO source pill (FR-138).
2. A not-applicable field (e.g. Filter on a dark) renders blank/"—" with no chip, driven by the applicability matrix — not inferred from data absence.
3. A real 0 renders "0" WITH its source pill.

## Rerun set (minimal)

- Layer-1: `plan_review_apply_with_audit` (journeys.rs)
- Layer-2: `inbox_ui_missing_path_attribute_banner_blocks_confirm`, `inbox_ui_confirm_does_not_move_then_apply_moves_to_shown_destination` (inbox_ui_journeys.rs)
- Manual-Windows: `journey-02-*` (inbox move)
- Coverage-matrix: #3, #18
