# q27-f5 — J02-ingest-review-reclassify-confirm-move: Inbox confirm suggests framing/project attribution (NEW)

Task: q27-f5 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

At the Inbox confirm gate, an attribution pass matches each new light session against existing framings/projects and returns ranked `IngestionAttributionCandidate`s (add-to-framing / new-framing / flag-optic-difference / new-project). Suggest-never-auto-merge; the user picks. This is a NEW confirm-flow stage.

## Stages hit

- NEW stage inside Stage 4 "Confirm turns a classified item into a plan" — attribution runs as the FIRST pre-ingest pass and surfaces ranked suggestions before a plan is generated. Journey doc update needed.

## Reviewer verification

1. Ingest a session matching an existing framing (target+train+pointing+rotation within tolerance); assert that framing is the TOP-ranked candidate by matchScore.
2. A session matching the project target but a different optic-train → `flag_optic_difference` candidate with opticMismatch=true.
3. No match → `new_project`/unassigned candidate.
4. Negative: nothing is merged/persisted by the pass itself (FR-019/FR-020) — suggestion surface only.

## Rerun set (minimal)

- Layer-1: F-Framing-8 Layer-1 (attribution ranking + user-pick-only apply)
- Layer-2: gap — authored in F-Framing-9 Windows-E2E; extend `inbox_ui_*` for the candidate UI
- Manual-Windows: F-Framing-9 (to be authored) + `journey-02-*` confirm stage
- Coverage-matrix: #3 (extend)
