# q15-t125 — J01-first-run-setup-data-sources: Source register/enable/disable/remap/rescan audited

Task: q15-t125 · Status: merged-spec
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

Registering, enabling/disabling, remapping, deleting a source, and rescans now write durable audit rows (`first_run.rs` previously bus-only).

## Stages hit

- Stage 7 "Rescan a folder … Remap … Disable … Delete (un-register)" — each writes a durable audit row.

## Reviewer verification

1. Disable then re-enable a source; assert two `audit_log_entry` rows with before→after state.
2. Apply a remap; assert a durable row recording old→new path (record re-point only, no file movement).
3. Rescan a folder; assert a durable row (user-initiated = workflow severity).
4. Negative: an automatic/periodic rescan writes a diagnostic-severity row, not a workflow one (severity matrix, data-model.md).

## Rerun set (minimal)

- Layer-1: `first_run_resolve_create_project` (journeys.rs)
- Layer-2: gap — `register_light_root`/`register_project_root` are helpers in `inbox_ui_journeys.rs`; no dedicated Data-Sources L2
- Manual-Windows: `journey-01-*` (first-run setup)
- Coverage-matrix: #1
