# q15-t123 — J01-first-run-setup-data-sources: Source protection override is audited

Task: q15-t123 · Status: merged-spec
Wave 0 grilling: Q15 · durable audit unification (#647, FR-130–FR-134, spec §8.3)

## Behavior delta (this journey)

Setting/removing a per-source protection override now writes a durable audit row with the returned `auditId` resolvable in the Audit Log.

## Stages hit

- Stage 7 "Ongoing management (Settings → Data Sources) … Override protection (set → visible change → and backend readback agrees → remove override)".

## Reviewer verification

1. Set a source protection override; assert the returned `auditId` resolves to an `audit_log_entry` row (not just a bus event).
2. Remove the override; assert a second durable row with the reverse before→after.
3. Negative: reading the Data Sources pane produces no audit row.

## Rerun set (minimal)

- Layer-1: `first_run_resolve_create_project` (journeys.rs)
- Layer-2: gap — `register_light_root`/`register_project_root` are helpers in `inbox_ui_journeys.rs`; no dedicated Data-Sources L2
- Manual-Windows: `journey-01-*` (first-run setup)
- Coverage-matrix: #1
