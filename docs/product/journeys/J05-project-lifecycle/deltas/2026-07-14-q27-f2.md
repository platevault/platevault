# q27-f2 — J05-project-lifecycle: Project light sessions cluster into framings (NEW)

Task: q27-f2 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

A project's light sessions are clustered into suggested **framings** (target + optic-train + pointing + rotation within tolerance) — multi-night/multi-filter data collapses to one framing. This is a NEW behavior with no existing J5 stage; it sits between attaching sources (Stage 2) and reviewing numbers (Stage 3).

## Stages hit

- NEW stage (between Stage 2 "Attach sources" and Stage 3 "Review the real numbers") — "a project's light sessions are grouped into framings". Journey doc update needed.

## Reviewer verification

1. Attach L/R/G/B sessions across two nights, one target+train, same pointing; assert they collapse into ONE suggested framing (clustering=suggested).
2. Two sessions beyond pointing tolerance → distinct framings.
3. Defaults (research R11a): pointing 10% FOV diagonal, rotation 3°, no-FOV fallback 0.2°; assert a fallback-flagged tolerance snapshot when equipment FOV is absent.
4. NULL-geometry (legacy) sessions are excluded and surface as unassigned.

## Rerun set (minimal)

- Layer-1: F-Framing-8 Layer-1 (multi-night/multi-filter collapse; NULL-geometry exclusion)
- Layer-2: gap — L2 framing coverage authored in F-Framing-9
- Manual-Windows: F-Framing-9 Windows-E2E framing scenario (to be authored)
- Coverage-matrix: #7 (extend)
