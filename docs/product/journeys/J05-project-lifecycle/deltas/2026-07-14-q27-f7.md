# q27-f7 — J05-project-lifecycle: Per-framing source view + manifest (NEW, blocked)

Task: q27-f7 · Status: blocked-on-Q20/Q10
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

Per-framing source views (one processing-tool-ready folder per framing) and per-framing manifests are reproducible projections of the framing model — the app groups and prepares but never stitches/integrates (§III). BLOCKED on the Q20 (spec-026/049) and Q10 (spec-024) iterations.

## Stages hit

- NEW stage — extends Stage 5 "Source views: the Generate dialog … produces a reviewable plan" and Stage 4 "Manifests & notes" to be per-framing. Journey doc update needed.

## Reviewer verification

1. (When unblocked) Generate a source view for one framing; assert a reviewable plan scoped to that framing's members only.
2. Assert a per-framing manifest renders as a reproducible projection (not a stored artifact).
3. Negative (§III): the app never stitches/registers/integrates framings.

## Rerun set (minimal)

- Layer-1: F-Framing-8 Layer-1 (blocked)
- Layer-2: gap — authored in F-Framing-9
- Manual-Windows: F-Framing-9 (to be authored)
- Coverage-matrix: #7 (extend)
