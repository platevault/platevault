# q27-f3 — J05-project-lifecycle: Merge/split/reassign framings (NEW)

Task: q27-f3 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

The user can merge, split, or reassign sessions between framings; the change persists and the framing flips to `user_adjusted` (never overwritten by re-derivation). Membership-only mutation — no filesystem plan.

## Stages hit

- NEW stage (extends the framing-grouping stage) — "adjust the suggested clustering". Journey doc update needed.

## Reviewer verification

1. Merge two framings; assert persistence and `clustering=user_adjusted`.
2. Split / reassign; re-run derivation and assert the `user_adjusted` framing is NOT modified (only new suggestions surface).
3. Assert no filesystem plan is generated (membership is DB metadata).

## Rerun set (minimal)

- Layer-1: F-Framing-8 Layer-1 (merge/split/reassign persistence + user_adjusted)
- Layer-2: gap — authored in F-Framing-9
- Manual-Windows: F-Framing-9 (to be authored)
- Coverage-matrix: #7 (extend)
