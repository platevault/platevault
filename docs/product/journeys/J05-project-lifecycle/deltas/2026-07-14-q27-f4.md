# q27-f4 — J05-project-lifecycle: Mosaic-mode flag on project create/update (NEW)

Task: q27-f4 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

A project carries an `is_mosaic` flag settable at create/update. When set, framings inherit the project's declared target and per-frame OBJECT/coordinate resolution is suppressed (no panel entity, no OBJECT string parsing).

## Stages hit

- Stage 1 "Create (/projects/new): name the project, optionally pick a processing-tool profile" — a mosaic toggle is added here (NEW field). Journey doc update needed.

## Reviewer verification

1. Create a project with is_mosaic=true; ingest two panels with different pointing/rotation; assert two framings both inheriting the declared target.
2. Assert per-frame OBJECT/coordinate resolution is suppressed (no per-panel target re-resolution).
3. Negative: no panel entity is created; no OBJECT/panel-name string is parsed (FR-018).

## Rerun set (minimal)

- Layer-1: F-Framing-8 Layer-1 (mosaic multi-framing inherit target, no OBJECT parsing)
- Layer-2: gap — authored in F-Framing-9
- Manual-Windows: F-Framing-9 (to be authored)
- Coverage-matrix: #7 (extend)
