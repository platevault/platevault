# q27-f11 — J05-project-lifecycle: Tolerance changes affect clustering

Task: q27-f11 · Status: merged-spec
Wave 0 grilling: Q27 · framing layer (008 US5–US7, FR-013–FR-022; cross-spec 006/009)

## Behavior delta (this journey)

Changing the clustering tunables immediately changes how a project's sessions cluster into framings.

## Stages hit

- NEW stage (the framing-grouping stage) — the grouping key is tolerance-driven, not exact-match.

## Reviewer verification

1. Widen pointing tolerance; assert two previously-distinct framings now collapse.
2. Negative: re-derivation still never modifies a `user_adjusted` framing.

## Rerun set (minimal)

- Layer-1: `lifecycle_integrity` (journeys.rs)
- Layer-2: `projects_ui_wizard_creates_real_folders_and_blocks_duplicate_name` (lifecycle_ui_journeys.rs)
- Manual-Windows: `journey-05-*` (project lifecycle)
- Coverage-matrix: #7, #8, #9
