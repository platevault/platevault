[package]
name = "targeting_catalogs"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
# Workspace deps
serde = { workspace = true }
serde_json.workspace = true
thiserror.workspace = true
time = { workspace = true }
tokio.workspace = true
tracing = { workspace = true }
uuid = { workspace = true }
# HTTP client — already in workspace lockfile via tauri; behind CatalogFetcher trait.
reqwest.workspace = true
# SHA-256 checksum verification — already in workspace lockfile via ring/sqlx.
sha2.workspace = true
hex.workspace = true
# Async trait object support for CatalogFetcher.
async-trait.workspace = true
# Minisign signature verification (MIT, D5, FR-026).
minisign.workspace = true

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
