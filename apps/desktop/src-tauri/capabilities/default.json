{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the desktop app. Grants the main window (and spec-020 multi-window children, label `alm-win-*`) the ability to invoke the spec 002 lifecycle commands, create additional webview windows, plus tauri-specta's built-in event registration.",
  "windows": ["main", "alm-win-*"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "dialog:default",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir",
    "mcp-bridge:default"
  ]
}
