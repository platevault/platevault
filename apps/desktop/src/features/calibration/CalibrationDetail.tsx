import { CalibrationPage } from './CalibrationPage';

/**
 * Route component for /calibration/$id.
 * Renders the full CalibrationPage which picks up the $id param internally.
 */
export function CalibrationDetail() {
  return <CalibrationPage />;
}
