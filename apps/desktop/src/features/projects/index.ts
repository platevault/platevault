export { ProjectsPage } from './ProjectsPage';
// ProjectDetailContent is the spec 008 real-data component (accepts projectId string).
// ProjectDetail is re-exported as an alias for smoke test compatibility.
export { ProjectDetailContent as ProjectDetail } from './ProjectDetail';
export { ProjectDetailContent } from './ProjectDetail';
// spec 043 (tasks #73/#43): the narrow ProjectsList sidebar was replaced by a
// full-width dense sortable table adopting the shared list-page layout.
export { ProjectsTable } from './ProjectsTable';
