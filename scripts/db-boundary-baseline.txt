# DB boundary baseline — production sqlx query/exec sites OUTSIDE crates/persistence/db.
# SEALED AT ZERO: this file must contain no count rows. All production SQL lives in
# crates/persistence/db; new queries are added there as repository methods, never here.
# Generated by scripts/check-db-boundary.sh --generate (refuses to record any leakage).
